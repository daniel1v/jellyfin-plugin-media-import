name: 'Test Plugin'

on:
  push:
    branches:
      - main
      - jellyfin-12
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches:
      - main
      - jellyfin-12
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/test.yaml@master
    with:
      dotnet-version: '10.0.x'
