{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "coreclr",
            "name": "Attach to test Jellyfin",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}
