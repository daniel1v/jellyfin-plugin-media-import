{
    // The data directory of an isolated Jellyfin test instance. It must contain
    // a .media-import-test marker file before deployment is allowed.
    "mediaImportTestDataDir": "${env:JELLYFIN_TEST_DATA_DIR}",
    "mediaImportPluginName": "Jellyfin.Plugin.MediaImport"
}
